services:

  fastapi:
    ports:
      - 3000:80
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: '1'
          memory: 1500M
        reservations:
          cpus: '1'
          memory: 1500M

networks:
  traefik-public:
    external: false
